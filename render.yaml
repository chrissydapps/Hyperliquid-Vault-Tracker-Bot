services:
  - type: web
    name: hyperliquid-telegram-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python hyperliquid_telegram_bot.py
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
